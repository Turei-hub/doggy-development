{
  "name": "doggy-development",
  "version": "1.0.0",
  "private": true,
  "description": "Website and gallery for Doggy Development, a dog walking business on Auckland's North Shore.",
  "type": "module",
  "scripts": {
    "serve": "firebase serve --only hosting",
    "test": "firebase emulators:exec --only firestore --project dd-test \"node firestore.rules.test.mjs\"",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.18.0",
    "firebase-tools": "^14.0.0"
  }
}
